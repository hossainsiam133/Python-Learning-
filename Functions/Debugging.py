def add(*num):
    sum = 0
    for x in num:
        sum += x
    return sum


print(add(10, 20))
# Python Debugging in VS Code
# This script is used to demonstrate debugging features in VS Code.
# You can add breakpoints by clicking on the left margin.
# Use F5 to start debugging.
# Use F10 to Step Over, F11 to Step Into, and Shift+F11 to Step Out.
# Use the Debug Console to inspect variables or run expressions.
# Great for understanding how the code executes line by line.
